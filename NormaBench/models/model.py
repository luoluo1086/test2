import requests

class MyModel:
    def __init__(self, api_key: str):
        self.api_key = api_key
        self.url = "https://"
        
    def predict(self, input_text: str) -> str:
        response = None  # TODO
        return response
